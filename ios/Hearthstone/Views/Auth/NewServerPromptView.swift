import SwiftUI

// Stub — full implementation lands in Task 17.
struct NewServerPromptView: View {
    let host: String
    let onConfirm: () -> Void
    let onCancel: () -> Void

    var body: some View {
        EmptyView()
    }
}
