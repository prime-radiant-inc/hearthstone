// HearthstoneApp.swift
// Hearthstone

import SwiftUI

@main
struct HearthstoneApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Hearthstone")
                .font(Theme.heading(28))
                .foregroundColor(Theme.hearth)
        }
    }
}
